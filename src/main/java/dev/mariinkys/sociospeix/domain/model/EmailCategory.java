package dev.mariinkys.sociospeix.domain.model;

public enum EmailCategory {
    CAMPAIGN,      // newsletters, sendToAll, sendToInterests, sendToMember
    TRANSACTIONAL  // password resets, and any future one-off system emails
}
